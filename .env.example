# Optionnel : en production (Vercel, etc.), URL absolue du backend.
# Si absent en build prod, voir DEFAULT_PRODUCTION_API dans src/lib/api.ts.
# VITE_API_BASE_URL=https://briceincode-sigis-backend.hf.space
