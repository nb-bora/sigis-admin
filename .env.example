# Optionnel : en production sur Vercel, surcharger l’URL du backend Render.
# Si absent, le build utilise https://sigis-backend.onrender.com (voir src/lib/api.ts).
# VITE_API_BASE_URL=https://sigis-backend.onrender.com
